const { getAll } = require("../model/ResearchModel");

const getAllResearch = (req, res) => {
  res.json({ todo: "this fonction" });
};

const getResearchByName = (req, res) => {
  res.json({ todo: "this fonction" });
};

const addResearch = (req, res) => {
  res.json({ todo: "this fonction" });
};

const updateResearch = (req, res) => {
  res.json({ todo: "this fonction" });
};

const deleteResearch = (req, res) => {
  res.json({ todo: "this fonction" });
};

module.exports = {
  getAllResearch,
  getResearchByName,
  addResearch,
  updateResearch,
  deleteResearch,
};
